# Task Completion

- For code changes in this app, run `rtk npm run lint`, `rtk npm run test`, and `rtk npm run build` when scope justifies full verification.
- For UI changes, also run the dev server and inspect core screens in a browser when feasible.
- Do not treat AI review provider UI as production automation unless backend/MCP wiring is changed too.